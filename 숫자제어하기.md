```javascript
# 정수 
value %1 ===0 

# 숫자인지 체크 
isNaN(number) //false 면 number true면 number가 아니다.

#0부터 시작하는 숫자 0제거하기
number.replace('/(^0+)/','');

# 절대값으로 숫자만들기
Math.abs(number)

# 소수점 제거 
Math.floor(number)
Math.ceil(number)
Math.round(number)
```

